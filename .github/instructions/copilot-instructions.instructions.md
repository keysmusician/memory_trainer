---
applyTo: '**'
---
For all style objects, use single quoted keys.
Style objects must satisfy JSX.CSSProperties, so CSS properties should be in kebob-case.
For example, use `background-color` instead of `backgroundColor`.
Do not use any CSS properties that are not supported by SolidJS.
